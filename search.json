[{"title":"【数据结构】第一篇","url":"/post/82ff7338.html","content":"\n本篇概述什么是数据结构、数据结构与算法的关系以及相关概念术语。 \n\n一、笔记&ensp;&ensp; &ensp; 数据结构&ensp;&ensp; &ensp; 数据 指能输入到计算机中并被处理的符号的总称。&ensp;&ensp; &ensp; 数据元素 是数据的基本单位，性质相同的同一类数据元素称为 数据对象。&ensp;&ensp; &ensp; 数据类型 指数据对象集合和数据集合相关联的操作集。&ensp;&ensp; &ensp; 数据结构 通常包含数据逻辑结构、物理结构以及数据相关联的操作。逻辑结构：线性结构、树结构、图；物理存储：顺序、链接、散列、索引结构。\n&ensp;&ensp; &ensp; 算法分析&ensp;&ensp; &ensp; 算法 通常包含有穷性、确定性、可行性三个性质，以一组有限的指令集完成某个目标并产生输出。本书涉及的部分算法：穷举法、贪心法、分治法、回溯法。&ensp;&ensp; &ensp; 空间复杂度 $S(n)$: 算法程序在执行时占用存储单元的长度。&ensp;&ensp; &ensp; 时间复杂度 $S(n)$: 算法程序在执行时耗费时间的长度。&ensp;&ensp; &ensp; 算法分析时通常参考最坏情况复杂度、平均复杂度，平均复杂度难以度量，通常评估算法的好坏参考最坏情况复杂度。&ensp;&ensp; &ensp; 复杂度渐进表示法&ensp;&ensp; &ensp; $T(N) = O(f(N))$ 表示存在常数 $C &gt; 0, n_0 &gt; 0$ 使得当 $n \\ge n_0$ 时有 $T(N) \\leq Cf(N)$&ensp;&ensp; &ensp; $T(N) = \\Omega (g(N))$ 表示存在常数 $C &gt; 0, n_0 &gt; 0$ 使得当 $n \\ge n_0$ 时有 $T(N) \\ge Cf(N)$&ensp;&ensp; &ensp; $T(N) = \\Theta (h(N))$ 表示同时有 $T(N)==O(h(N))、T(N) = \\Theta(h(N))$ \n&ensp;&ensp; &ensp; 算法分析 若 $T_1(N) = O(f(N))、T_2(N) = O(g(N))$，则有：&ensp;&ensp; &ensp; $T_1(N) + T_2(N) = max(O(f(N)), O(g(N)))$&ensp;&ensp; &ensp; $T_1(N) \\cdot T_2(N) = O(f(N) \\cdot g(N))$&ensp;&ensp; &ensp; 若 $T(N)$是关于 n 的 k 阶多项式，则 $T(N)=\\Theta(n^k)$，表示复杂度只受高阶影响。\n二、习题&ensp;&ensp; &ensp; 1. 最大子列和问题&ensp;&ensp; &ensp; 给定 N 个整数的序列 {$A_1, A_2, … ,A_N$}，求函数 $ \\sum\\limits_{k=i}^j A_k $ 最大值。\n\n方法一：穷举法int MaxSubSeqSum1(int List[], int N) &#123;\n    int ThisSum, MaxSum;\n    for (int i = 0; i &lt; N; i++) &#123;        // i 为子列左端\n        ThisSum = 0;\n        for (int j = i; j &lt; N; j++) &#123;    // j 为子列右端\n            ThisSum += List[j];\n            if (ThisSum > MaxSum)\n                MaxSum = ThisSum;\n        &#125;\n    &#125;\n    return MaxSum;\n&#125;\n方法二：分治法int Max3(int A, int B, int C) &#123;\n  return (A > B) ? (A > C ? A : C) : (B > C ? B : C);\n&#125;\n\nint DivideAndConquer(int List[], int left, int right) &#123;\n  int MaxLeftSum, MaxRightSum;              // 左右子问题解\n  int MaxLeftBorderSum, MaxRightBorderSum;  // 跨跃分界线解\n\n  int LeftBorderSum, RightBorderSum;\n  int center, i;\n\n  // 递归终止\n  if (left == right) &#123;\n    if (List[Left] > 0)&#123;\n      return List[Left];\n    &#125; else &#123;\n      return 0;\n    &#125;\n  &#125;\n\n  // 分治\n  center = (left + right) / 2;\n  MaxLeftSum = DivideAndConquer(List, Left, center);\n  MaxRightSum = DivideAndConquer(List, center+1, right)\n\n  // 求跨界的最大子列和\n  MaxLeftBorderSum = 0; LeftBorderSum = 0;\n  for (int i = center; i >= left; i++) &#123;\n    LeftBorderSum += List[i];\n    if (LeftBorderSum > MaxLeftBorderSum)&#123;\n      MaxLeftBorderSum = LeftBorderSum;\n    &#125;\n  &#125;  // 左边扫描结束\n\n  MaxRightBorderSum = 0; RightBorderSum = 0;\n  for (int i = center + 1; i &lt;= right; i++) &#123;\n    RightBorderSum += List[i];\n    if (RightBorderSum > MaxRightBorderSum) &#123;\n      MaxRightBorderSum = RightBorderSum;\n    &#125;\n  &#125;  // 右边扫描结束\n\n  return Max3(MaxLeftSum, MaxRightSum, MaxLeftBorderSum + MaxRightBorderSum);\n&#125;\n\nint MaxSubSeqSum2(int List[], int N) &#123;\n  return DivideAndConquer(List, 0, N - 1);\n&#125;\n方法三：在线处理int MaxSubSeqSum3(int List[], int N) &#123;\n  int ThisSum = MaxSum = 0;\n  for (int i = 0; i &lt; N; i++) &#123;\n    ThisSum += List[i];\n    if (ThisSum > MaxSum) &#123;\n      MaxSum = ThisSum;\n    &#125; else if (ThisSum &lt; 0) &#123;\n      ThisSum = 0;\n    &#125;\n  &#125;\n  return MaxSum;\n&#125;\n\n\n// Main 方法\n#include &lt;stdio.h>\n#define MAXN 100000    //本题最大数据是十万\nint main(void) &#123;\n    int K, i;\n    int a[MAXN] = &#123;0&#125;;\n\n    scanf(\"%d\", &amp;K);\n    for ( i = 0; i &lt; K; i++ )\n        scanf(\"%d\", &amp;a[i]);\n    printf(\"%d\", MaxSubseqSum4( a, K ));\n\n    return 0;\n&#125;\n\n&ensp;&ensp; &ensp; 2. Maximum Subsequence Sum&ensp;&ensp; &ensp; 给定 K 个整数的序列 {$N_1, N_2, … ,N_K$}，求最大子序列和 {$N_i, N_i+1, …, N_j$}，子序列满足条件 $1 \\leq i \\leq j \\leq K$，当输入序列全是负数时，输出最大序列和为 0，输出序列第一个数和最后一个数。\nimport java.util.Scanner;\npublic class Main &#123;\n\n    public static void main(String[] args) &#123;\n        Scanner scanner = new Scanner(System.in);\n        int k = scanner.nextInt();\n\n        int[] list = new int[k];\n        for (int i = 0; i &lt; k; i++) &#123;\n            list[i] = scanner.nextInt();\n        &#125;\n        MaxSubseqSum(list);\n    &#125;\n\n    public static void MaxSubseqSum(int[] list) &#123;\n        int MaxSum = list[0], thisSum = 0;\n\n        int fristIndex = 0, lastIndex = 0;\n        for (int i = 0; i &lt; list.length; i++) &#123;\n            thisSum += list[i];\n            if (thisSum > MaxSum) &#123;\n                lastIndex = i;\n                MaxSum = thisSum;\n            &#125; else if (thisSum &lt; 0) &#123;\n                thisSum = 0;\n            &#125;\n        &#125;\n        \n        if (MaxSum &lt; 0) &#123;\n            MaxSum = 0;\n            fristIndex = 0;\n            lastIndex = list.length - 1;\n        &#125; else &#123;\n            int temp = MaxSum;\n            for (int j = lastIndex; j >= 0; j--) &#123;\n                temp -= list[j];\n                if (temp == 0) &#123;\n                    fristIndex = j;\n                &#125;\n            &#125;\n        &#125;\n\n        System.out.println(MaxSum + \" \" + list[fristIndex] + \" \" + list[lastIndex]);\n    &#125;\n&#125;\n\n&ensp;&ensp; &ensp; 3. 二分查找&ensp;&ensp; &ensp; 实现二分查找算法。\n\n裁判测试程序样例，本题仅编写二分查找算法即可#include &lt;stdio.h>\n#include &lt;stdlib.h>\n\n#define MAXSIZE 10\n#define NotFound 0\ntypedef int ElementType;\n\ntypedef int Position;\ntypedef struct LNode *List;\nstruct LNode &#123;\n    ElementType Data[MAXSIZE];\n    Position Last; /* 保存线性表中最后一个元素的位置 */\n&#125;;\n\nList ReadInput(); /* 裁判实现，细节不表。元素从下标1开始存储 */\nPosition BinarySearch( List L, ElementType X );\n\nint main()\n&#123;\n    List L;\n    ElementType X;\n    Position P;\n\n    L = ReadInput();\n    scanf(\"%d\", &amp;X);\n    P = BinarySearch( L, X );\n    printf(\"%d\\n\", P);\n\n    return 0;\n&#125;\n\n/* 你的代码将被嵌在这里 */\nPosition BinarySearch(List L, ElementType X) &#123;\n  int head = 1, end = L->Last;\n  int m, flag = -1;\n  while (head &lt;= end) &#123;\n    m = (head + end)/2;\n    if ( L->Data[m] == X) &#123;\n      flag = m;\n      break;\n    &#125; else if (L->Data[m] > X) &#123;\n      end = m - 1;\n    &#125; else if (L->Data[m] &lt; X) &#123;\n      head = m + 1;\n    &#125;\n  &#125;\n  if (flag == -1) &#123;\n    return NotFound;\n  &#125; else &#123;\n    return flag;\n  &#125;\n&#125;\n\n\n&ensp;&ensp; &ensp; 分治法复杂度分析&ensp;&ensp; &ensp; 时间复杂度&ensp;&ensp; &ensp; &ensp; $T(N)=2T(N/2)+N$&ensp;&ensp; &ensp; &ensp; $T_1 = 1$&ensp;&ensp; &ensp; &ensp; $T_2 = 4 = 2 \\cdot 2$&ensp;&ensp; &ensp; &ensp; $T_4 = 12 = 4 \\cdot 3$&ensp;&ensp; &ensp; &ensp; 可推导至一般形式 $T(2^k)=2^k \\cdot (k+1)$&ensp;&ensp; &ensp; &ensp; 若分割至 N，则 $T(N) = N \\cdot (k+1)$，带入 $k=logN$&ensp;&ensp; &ensp; &ensp; $T(N)=NlogN+N=O(NlogN)$&ensp;&ensp; &ensp; 空间复杂度&ensp;&ensp; &ensp; &ensp; 递归而产生的空间复杂度 = 每次递归的空间复杂度 * 递归深度，单次递归需要的空间是一个与 N 无关的常量，计为 $O(1)$, 分成 2 个、4 个、N 个，则需要分 logN 次，则 $S(n)=O(logN)$&ensp;&ensp; &ensp; &ensp; 算法整体复杂度：n 个数据，求左边空间要 n/2， 右边要 n/2，跨越分界线需要扫描一次，则 $S(n)=2n=O(n)$&ensp;&ensp; &ensp; &ensp;&ensp;&ensp; &ensp; &ensp; \n","categories":["Code"],"tags":["数据结构"]}]